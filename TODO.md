# GitPorter 开发计划

## 已完成

1. ~~**项目结构重构** — 文件拆分，解耦~~ ✅
2. ~~**`config` 子命令** — 交互式生成配置，`--show` 脱敏查看~~ ✅
3. ~~**`list` 子命令** — 列出仓库，支持 `--filter` / `--exclude` 过滤~~ ✅
4. ~~**`sync --dry-run`** — 预览模式 + 确认提示~~ ✅
5. ~~**进度条** — rich 进度条，成功/失败颜色区分~~ ✅
6. ~~**增量同步** — 自动判断 clone / fetch，`--force-reclone` 强制重建~~ ✅
7. ~~**容器化适配** — Dockerfile / docker-compose / CI 自动构建镜像~~ ✅
8. ~~**运行日志密钥脱敏** — git URL、API 响应中的 token 自动打码~~ ✅
9. ~~**`config --validate`** — 配置校验（结构 → API 连通 → Git 连通）~~ ✅
10. ~~**`sync --select`** — 交互式仓库选择，支持序号/范围/排序切换~~ ✅
11. ~~**`list` 交互式排序** — `s` 键循环切换：名称/最近推送/最近更新/创建时间~~ ✅
12. ~~**`sync --select` 搜索过滤** — `/` 搜索模式，输入即过滤仓库名~~ ✅
13. ~~**`sync --retry`** — 自动保存失败记录，`--retry` 只重试失败仓库~~ ✅
14. ~~**`list --status`** — 显示同步状态，标记已同步到目标平台的仓库~~ ✅
15. ~~**并发同步 `--workers N`** — 多线程并行同步，缩短大批量迁移时间~~ ✅
16. ~~**`list --limit N`** — 快速预览前 N 个仓库~~ ✅
17. ~~**`sync --repos`** — 命令行直接指定仓库名，无需交互~~ ✅
18. ~~**`list --search`** — 关键词搜索仓库名和描述~~ ✅
19. ~~**同步耗时统计** — 完成后显示总耗时~~ ✅
